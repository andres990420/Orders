@page "/categories/edit/{Id:int}"

<h3>Editar Pais</h3>
@if (category is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="categoryForm" Label="Pais:" Model="category" OnValidSubmit="EditAsync" ReturnAction="Return" />
}